@model List<Generwell.Web.ViewModels.WellViewModel>
@using Generwell.Modules.GenerwellConstants;

<script type="text/javascript">
    $(document).ready(function () {
        var targetUrl = '/WellLineReport/Index/';
        wellPage.initialize(targetUrl);
    });
</script>


<table id="wellListTableId" class="table-striped table table-bordered table-hover wellListDataTable dataTable no-footer" style="width:100%;">
    <thead>
        <tr>
            <th>Id<i class="fa fa-fw fa-sort"></i></th>
            <th>Well Name<i class="fa fa-fw fa-sort"></i></th>
            <th>Bottom Hole Location<i class="fa fa-fw fa-sort"></i></th>
            <th>Surface Hole Location<i class="fa fa-fw fa-sort"></i></th>
            <th>Active Task Count<i class="fa fa-fw fa-sort"></i></th>
            <th>Lattitude<i class="fa fa-fw fa-sort"></i></th>
            <th>Longitude<i class="fa fa-fw fa-sort"></i></th>
            <th>IsFavorite<i class="fa fa-fw fa-sort"></i></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            @foreach (var item in Model)
            {
                <tr class="gradeU">
                    <td>@item.id</td>
                    <td>@item.name</td>
                    <td>@item.bottomHoleLocation</td>
                    <td>@item.surfaceHoleLocation</td>
                    <td>@item.activeTaskCount</td>
                    <td>@item.latitude</td>
                    <td>@item.longitude</td>
                    <td>@item.isFavorite</td>
                </tr>
            }
        }
        else
        {
            <tr class="gradeU">
                <td>@GenerwellConstants.Constants.NoData</td>
                <td>@GenerwellConstants.Constants.NoData</td>
                <td>@GenerwellConstants.Constants.NoData</td>
                <td>@GenerwellConstants.Constants.NoData</td>
                <td>@GenerwellConstants.Constants.NoData</td>
                <td>@GenerwellConstants.Constants.NoData</td>
            </tr>

        }
    </tbody>
</table>





