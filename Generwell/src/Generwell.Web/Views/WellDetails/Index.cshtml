@model List<Generwell.Web.ViewModels.FieldsViewModel>
@using Generwell.Modules.GenerwellConstants;

@{
    ViewBag.Title = "Well Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        wellDetailsPage.initialize(@GenerwellConstants.Constants.WellId);
    });
</script>

<!-- Loading Modal Start here-->
@Html.Partial("_LoadingPopup")
<!-- Modal ends Here -->

<div class="wrapper wrapper-content animated fadeInRight mr-20 ml-20">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h2> Well Details</h2>
                </div>
                <hr />
                <div>
                    <button id="backDetailsPageId" class="btn btn-primary" type="button" data-toggle="Back">Back</button>
                    <button id="locationPageId" class="btn btn-primary" type="button" data-toggle="Back">Location</button>
                    <button id="taskPageId" class="btn btn-primary" type="button" data-toggle="Back">Tasks</button>
                </div>
                <br />
                <div>
                    <span class="ml-20"><input type="checkbox" id="followWellId" name="followWell" class="ui-checkboxradio-checked" @GenerwellConstants.Constants.IsFollow value=""> Follow Well<br></span>
                </div>
                <hr />
                <br />
                <div class="ibox-content">
                    <table id="wellDetailsListTableId" class="table table-striped table-bordered table-hover wellDetailsListTableDataTable" style="width:100%;">
                        <thead>
                            <tr>
                                <th class="wd-width25">Id<i class="fa fa-fw fa-sort"></i></th>
                                <th class="wd-width25">Name<i class="fa fa-fw fa-sort"></i></th>
                                <th class="wd-width25">Value<i class="fa fa-fw fa-sort"></i></th>
                                <th class="wd-width25">Id<i class="fa fa-fw fa-sort"></i></th>
                                <th class="wd-width25">Name<i class="fa fa-fw fa-sort"></i></th>
                                <th class="wd-width25">Value<i class="fa fa-fw fa-sort"></i></th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Count() > 0)
                            {
                                int count = 1;
                                @foreach (var item in Model)
                                {
                                    @if (count % 2 == 1)
                                    {
                                        @:<tr class="gradeU">
                                            <td>@item.id</td>
                                            <td>@item.description</td>
                                            <td><label>@item.value</label></td>
                                    }
                                    @if (count % 2 == 0)
                                    {
                                        <td>@item.id</td>
                                                <td>@item.description</td>
                                                <td><label>@item.value</label></td>
                                            @:</tr>
                                            }
                                    count++;
                                }
                            }
                            else
                            {
                                <tr class="gradeU">
                                    <td>@GenerwellConstants.Constants.NoData</td>
                                    <td>@GenerwellConstants.Constants.NoData</td>
                                    <td>@GenerwellConstants.Constants.NoData</td>
                                </tr>

                            }
                        </tbody>
                    </table>                                       
                </div>
            </div>
        </div>
    </div>
</div>
