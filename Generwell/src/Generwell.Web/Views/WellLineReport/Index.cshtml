@model List<Generwell.Web.ViewModels.WellLineReportViewModel>
@using Generwell.Modules.GenerwellConstants;

@{
    ViewBag.Title = "Well Line Reports";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        var targetUrl = '/WellDetails/Index/';
        wellLineReportPage.initialize(targetUrl);
    });
</script>


<!-- Loading Modal Start here-->
@Html.Partial("_LoadingPopup")
<!-- Modal ends Here -->

<div class="wrapper wrapper-content animated fadeInRight mr-20 ml-20">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h2> Well Line Reports</h2>
                    @GenerwellConstants.Constants.WellName
                </div>
                <hr />               
                <br />
                <div>
                    <span class="ml-20"><input type="checkbox" id="followWellId" name="followWell" class="ui-checkboxradio-checked" @GenerwellConstants.Constants.IsFollow value=""> Follow Well<br></span>
                </div>
                <hr />
                <br/>
                <div class="ibox-content w-50">
                    <table id="wellLineReportListTableId" class="table table-striped table-bordered table-hover wellLineReportListDataTable" style="width:100%;">
                        <thead>
                            <tr>
                                <th>Id<i class="fa fa-fw fa-sort"></i></th>
                                <th>Well Line Report<i class="fa fa-fw fa-sort"></i></th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Count() > 0)
                            {
                                @foreach (var item in Model)
                                {
                                    <tr class="gradeU">
                                        <td>@item.id</td>
                                        <td>@item.name</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr class="gradeU">
                                    <td>@GenerwellConstants.Constants.NoData</td>
                                    <td>@GenerwellConstants.Constants.NoData</td>
                                </tr>

                            }
                        </tbody>
                    </table>                    
                </div>
                <div>
                    <button id="backLineReportId" class="btn btn-primary" type="button" data-toggle="Back">Back</button>
                    <button id="locationPageId" class="btn btn-primary" type="button" data-toggle="Back">Location</button>
                    <button id="taskPageId" class="btn btn-primary" type="button" data-toggle="Back">Tasks</button>
                </div>
            </div>
        </div>
    </div>
</div>


